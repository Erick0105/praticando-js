// Cria uma array e já adiciona os valores
var colors = ['white', 'black', 'custom']; 

// Mostra o primeiro item da array
var el = document.getElementById('colors');
el.textContent = colors[0];

//? array é como uma lista no back-end funciona praticamente do mesmo jeito